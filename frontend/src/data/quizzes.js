export const quizzes = {
  'how-web-works': [
    { question: 'What HTTP method should a login endpoint use?', options: ['GET', 'POST', 'PUT', 'DELETE'], correct: 1, explanation: 'POST is used because login creates something (a session/token) and sends sensitive data in the request body, not the URL.' },
    { question: 'What does a 401 status code mean?', options: ['Request succeeded', 'Resource not found', 'Not authenticated (who are you?)', 'Forbidden (not allowed)'], correct: 2, explanation: '401 means the server doesn\'t know who you are - missing or invalid token. 403 means it knows you but you lack permission.' },
    { question: 'In HTTP, the server remembers who you are between requests.', options: ['True - it keeps a connection open', 'False - each request is independent', 'Only with cookies', 'Only with websockets'], correct: 1, explanation: 'HTTP is stateless. Each request is independent. That\'s why we need tokens - they carry identity info with each request.' },
  ],
  'headers-cookies': [
    { question: 'Where does the JWT token travel in an API request?', options: ['URL query parameter', 'Request body', 'Authorization header', 'Cookie'], correct: 2, explanation: 'The standard pattern is Authorization: Bearer <token>. Query params would log the token in server logs. Cookies work but have CSRF risks.' },
    { question: 'What does "Bearer" mean in "Authorization: Bearer eyJ..."?', options: ['The token type is "bear"', 'The client is "bearing" (carrying) the token as proof', 'It\'s a specific encryption method', 'It\'s optional and can be omitted'], correct: 1, explanation: 'Bearer means "I\'m carrying this token as proof of identity." The server extracts everything after "Bearer " as the token.' },
    { question: 'Why should you NOT store JWTs in localStorage?', options: ['It\'s too slow', 'It\'s vulnerable to XSS attacks (any JS can read it)', 'localStorage doesn\'t persist', 'It\'s too small'], correct: 1, explanation: 'Any JavaScript on the page (including injected XSS scripts) can read localStorage. An in-memory variable or HTTP-only cookie is safer.' },
  ],
  'env-secrets': [
    { question: 'What happens if you accidentally commit a SECRET_KEY to GitHub?', options: ['Nothing - GitHub removes it automatically', 'You should delete the commit and it\'s fine', 'You must rotate (change) the secret immediately', 'It\'s OK if the repo is private'], correct: 2, explanation: 'Git history preserves deleted commits. Bots scan GitHub constantly. The only safe response is rotating the compromised secret immediately.' },
    { question: 'Which file should NEVER be committed to git?', options: ['.gitignore', 'requirements.txt', '.env', 'main.py'], correct: 2, explanation: '.env contains your actual secret values. .gitignore should include .env to prevent accidental commits.' },
    { question: 'What does python-dotenv do?', options: ['Encrypts your .env file', 'Loads .env variables into os.environ at runtime', 'Sends .env to a cloud vault', 'Validates environment variable types'], correct: 1, explanation: 'load_dotenv() reads the .env file and sets each key=value pair as an environment variable accessible via os.getenv().' },
  ],
  'api-design': [
    { question: 'A registration endpoint returns the user profile. Should it include the password?', options: ['Yes, for confirmation', 'Yes, but encrypted', 'No - never return passwords in responses', 'Only in development mode'], correct: 2, explanation: 'API responses should never contain passwords, hashes, or other secrets. Use response_model in FastAPI to control what\'s returned.' },
    { question: 'What does Pydantic do in FastAPI?', options: ['Handles routing', 'Validates request/response data automatically', 'Manages the database', 'Compresses JSON'], correct: 1, explanation: 'Pydantic models define the expected shape of request bodies and responses. Invalid data gets a 422 error before your code runs.' },
    { question: 'What status code should a successful registration return?', options: ['200 OK', '201 Created', '204 No Content', '301 Redirect'], correct: 1, explanation: '201 Created is the correct code when a new resource (user account) has been created. 200 OK is for successful reads.' },
  ],
  'authn-vs-authz': [
    { question: 'A user sends a valid JWT but tries to access an admin-only endpoint. What status code?', options: ['401 Unauthorized', '403 Forbidden', '404 Not Found', '500 Server Error'], correct: 1, explanation: '403 Forbidden - the user IS authenticated (valid token) but NOT authorized (wrong role). 401 would mean the token itself is invalid.' },
    { question: 'Which happens first?', options: ['Authorization then authentication', 'Authentication then authorization', 'They happen simultaneously', 'It depends on the framework'], correct: 1, explanation: 'You must know WHO someone is (authentication) before you can check WHAT they\'re allowed to do (authorization).' },
    { question: 'In our projects, authentication is handled by ____ and authorization by ____.', options: ['get_current_user / require_role', 'require_role / get_current_user', 'bcrypt / jwt', 'jwt / bcrypt'], correct: 0, explanation: 'get_current_user verifies the JWT (authentication). require_role checks if the user\'s role is high enough (authorization).' },
  ],
  'password-hashing': [
    { question: 'Why is SHA-256 dangerous for password hashing?', options: ['It\'s too complex', 'It\'s too fast - GPUs compute 10 billion hashes/second', 'It doesn\'t produce enough output', 'It\'s been mathematically broken'], correct: 1, explanation: 'Speed is the enemy. bcrypt is intentionally slow (~5,000 hashes/sec on GPU) to make brute-force attacks impractical.' },
    { question: 'Two users both choose the password "password123". Their bcrypt hashes will be:', options: ['Identical - same input = same output', 'Different - each gets a unique random salt', 'Same hash but different length', 'It depends on the cost factor'], correct: 1, explanation: 'bcrypt generates a unique random salt per hash. Same password + different salt = different hash. This defeats rainbow tables.' },
    { question: 'The salt in bcrypt is:', options: ['Secret and stored separately', 'Embedded in the hash string and NOT secret', 'Sent to the client', 'Generated from the username'], correct: 1, explanation: 'The salt is visible in the hash string (characters 8-29). It\'s not secret - its purpose is to make each hash unique, not to be hidden.' },
    { question: 'What does "cost factor 12" mean in bcrypt?', options: ['12 characters of output', '12 bytes of salt', '2^12 = 4,096 iterations of the hash function', '12 seconds to compute'], correct: 2, explanation: 'The cost factor is the exponent. Cost 12 means 2^12 = 4,096 iterations. Each increase by 1 doubles the computation time.' },
  ],
  'jwt': [
    { question: 'Can anyone read the contents of a JWT token?', options: ['No - it\'s encrypted', 'Yes - the payload is just Base64 encoded, not encrypted', 'Only with the secret key', 'Only the server can'], correct: 1, explanation: 'JWTs are SIGNED, not ENCRYPTED. Anyone can Base64-decode the payload. The signature only proves it wasn\'t tampered with.' },
    { question: 'What does the "exp" claim in a JWT do?', options: ['Exports the token', 'Sets when the token expires', 'Explains the token purpose', 'Expands the payload size'], correct: 1, explanation: 'The "exp" (expiration) claim is a Unix timestamp. jwt.decode() automatically checks this and raises ExpiredSignatureError if past.' },
    { question: 'If you modify one character in a JWT\'s payload, what happens?', options: ['Nothing - it still works', 'The signature becomes invalid - verification fails', 'It auto-corrects', 'Only the modified field is wrong'], correct: 1, explanation: 'The signature is computed from the exact header and payload. Any change, even one character, produces a completely different signature.' },
    { question: 'What is the JTI claim used for?', options: ['JWT Type Identifier', 'A unique token ID for blacklisting/revocation', 'JavaScript Token Interface', 'JWT Timeout Interval'], correct: 1, explanation: 'JTI (JWT ID) is a unique UUID per token. On logout, we add the JTI to a blacklist. Every request checks if the token\'s JTI is blacklisted.' },
  ],
  'middleware': [
    { question: 'In FastAPI, Depends(get_current_user) runs:', options: ['After the endpoint function', 'Before the endpoint function - injecting the result', 'Only when explicitly called', 'In a separate thread'], correct: 1, explanation: 'FastAPI dependencies run before your endpoint. The result is injected as a parameter. If the dependency raises an exception, your endpoint never runs.' },
    { question: 'What is the advantage of Depends() over global middleware?', options: ['It\'s faster', 'You can choose which endpoints are protected', 'It works on all endpoints automatically', 'It doesn\'t need a token'], correct: 1, explanation: 'Depends() is per-endpoint. /register doesn\'t need auth, but /admin does. Global middleware would need an exclusion list.' },
    { question: 'What does OAuth2PasswordBearer do in FastAPI?', options: ['Encrypts passwords', 'Extracts the Bearer token from the Authorization header', 'Creates OAuth2 tokens', 'Stores tokens in cookies'], correct: 1, explanation: 'It\'s a helper that reads the Authorization header, strips the "Bearer " prefix, and gives you the raw token string.' },
  ],
  'rs256': [
    { question: 'If a resource server using HS256 is hacked, the attacker can:', options: ['Only read data', 'Forge tokens for any user', 'Only verify existing tokens', 'Nothing - HS256 is secure'], correct: 1, explanation: 'HS256 uses the same secret for signing AND verifying. If the secret leaks, the attacker can create valid tokens for any user.' },
    { question: 'With RS256, the resource server has:', options: ['The private key', 'The shared secret', 'Only the public key', 'Both keys'], correct: 2, explanation: 'The resource server only needs the public key (to verify). It cannot sign/forge tokens. This is the key security advantage.' },
    { question: 'Is it safe to expose the public key via an API endpoint?', options: ['No - keys should never be exposed', 'Yes - public keys can only verify, not sign', 'Only over HTTPS', 'Only to authenticated users'], correct: 1, explanation: 'That\'s why it\'s called a PUBLIC key. GET /api/public-key is safe because the public key cannot create tokens, only verify them.' },
  ],
  'rbac': [
    { question: 'In our RBAC hierarchy, if a user has the "admin" role, they can access:', options: ['Only admin-level resources', 'Admin + user + public resources', 'Everything including super_admin', 'Only what\'s explicitly assigned'], correct: 1, explanation: 'Hierarchical RBAC means higher roles inherit lower-role permissions. admin (level 2) can access admin (2), user (1), and public (0).' },
    { question: 'Where should RBAC be enforced?', options: ['Only in the frontend (hide UI elements)', 'Only in the backend (filter queries)', 'Both, but backend is the source of truth', 'In the database triggers'], correct: 2, explanation: 'The frontend hides buttons for UX, but the backend MUST enforce permissions. Attackers can always call the API directly.' },
    { question: 'require_role("admin") will reject a request from a user with role:', options: ['admin', 'super_admin', 'user', 'Both admin and super_admin'], correct: 2, explanation: 'user (level 1) is below admin (level 2). super_admin (level 3) would pass because it\'s above admin.' },
  ],
  'refresh-tokens': [
    { question: 'Why do access tokens have short lifetimes (30-60 minutes)?', options: ['To save database space', 'To limit the damage window if a token is stolen', 'Because JWTs can\'t last longer', 'For performance reasons'], correct: 1, explanation: 'Short-lived tokens limit exposure. If stolen, an access token is only useful for 30 minutes, not forever.' },
    { question: 'What happens to the old refresh token when you call /refresh?', options: ['It stays valid', 'It\'s revoked (marked is_revoked=1 in DB)', 'It\'s deleted from the client only', 'It expires naturally'], correct: 1, explanation: 'Refresh token rotation: the old token is revoked in the database immediately. This means a stolen token can only be used once.' },
    { question: 'Where should refresh tokens be stored?', options: ['localStorage (for persistence)', 'An HTTP-only cookie or secure server-side storage', 'In the JWT payload itself', 'In a URL parameter'], correct: 1, explanation: 'Refresh tokens are long-lived and powerful (they can get new access tokens). HTTP-only cookies are safest for browsers.' },
  ],
  'security-hardening': [
    { question: 'After 5 failed login attempts, our system:', options: ['Deletes the account', 'Locks the account for 15 minutes', 'Sends an email alert', 'Increases the bcrypt cost'], correct: 1, explanation: 'Account lockout sets locked_until = now + 15 minutes. Even correct passwords are rejected until the lock expires.' },
    { question: 'Token blacklisting works by:', options: ['Deleting the token from the client', 'Adding the token\'s unique JTI to a blacklist table', 'Changing the secret key', 'Shortening the expiry'], correct: 1, explanation: 'Each token has a unique JTI (UUID). On logout, we INSERT the JTI into token_blacklist. Every request checks this table.' },
    { question: 'The password reset endpoint returns the same message for existing and non-existing emails because:', options: ['It\'s a bug', 'It prevents email enumeration attacks', 'It\'s faster', 'The server doesn\'t check the email'], correct: 1, explanation: 'If different responses reveal whether an email is registered, attackers can discover valid accounts. Same message = no information leak.' },
  ],
  'rag-auth': [
    { question: 'In an authenticated RAG system, document access should be filtered:', options: ['After the LLM generates a response', 'Before retrieval - the LLM never sees unauthorized docs', 'By the LLM itself using a system prompt', 'Only in the frontend'], correct: 1, explanation: 'Pre-retrieval filtering is the only safe approach. If the LLM sees unauthorized docs (even to filter them), information can leak.' },
    { question: 'Why do we use separate ChromaDB collections per access level?', options: ['Performance optimization', 'Each collection enforces its own access level - complete isolation', 'ChromaDB requires it', 'It\'s easier to delete'], correct: 1, explanation: 'Separate collections mean a user role query never even touches higher-level documents. This is pre-retrieval filtering.' },
    { question: 'The FAISS index stores:', options: ['All documents regardless of access level', 'Only public documents (guest+ access)', 'Only admin documents', 'User credentials'], correct: 1, explanation: 'FAISS is used for the public knowledge base (product info, FAQs). Role-gated docs go in separate ChromaDB collections.' },
  ],
  'agentic-auth': [
    { question: 'What makes the ReAct pattern different from calling all tools at once?', options: ['It\'s faster', 'The agent reasons step-by-step, calling one tool at a time based on what it learns', 'It uses fewer API calls', 'It doesn\'t need an LLM'], correct: 1, explanation: 'ReAct = Reason + Act. The agent thinks about what info it needs, calls one tool, reads results, then decides the next step. This is true agency.' },
    { question: 'Tool gating in our agent is enforced:', options: ['Only before the loop (once)', 'Only inside the loop (per step)', 'Both before the loop AND inside each step', 'By the LLM\'s system prompt only'], correct: 2, explanation: 'Tools are filtered before the loop (agent never sees them) AND checked inside (in case the LLM hallucmates a tool name).' },
    { question: 'If a user with "user" role asks about infrastructure costs, the agent will:', options: ['Return an error immediately', 'Use public_search and internal_search only - admin_search is not available', 'Use admin_search anyway', 'Ask the user to upgrade their role'], correct: 1, explanation: 'The agent only has access to tools matching the user\'s role. For role=user: public_search and internal_search. admin_search doesn\'t exist in its tool list.' },
  ],
  'production': [
    { question: 'Which of these is NOT implemented in our 3 projects?', options: ['Token blacklisting', 'Account lockout', 'Rate limiting', 'Password reset'], correct: 2, explanation: 'Rate limiting (e.g., slowapi) is not implemented. All the other features are: blacklisting (P3), lockout (all), password reset (P2).' },
    { question: 'In production, allow_origins=["*"] in CORS is:', options: ['Fine - it works everywhere', 'Dangerous - should whitelist specific frontend URLs', 'Required for JWT auth', 'Only a problem with cookies'], correct: 1, explanation: 'Wildcard CORS allows any website to call your API. In production, whitelist only your actual frontend domain(s).' },
    { question: 'Our 3 projects demonstrate a progression from:', options: ['Frontend → Backend → Database', 'HS256 → RS256 → Token blacklisting (increasing security layers)', 'Python → JavaScript → SQL', 'Simple → Complex database schemas'], correct: 1, explanation: 'P1: HS256 + lockout. P2: RS256 + RBAC + password reset. P3: HS256 + JTI blacklisting + ReAct + audit logging. Each adds security layers.' },
  ],
}
